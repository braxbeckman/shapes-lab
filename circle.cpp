#include "circle.hpp"
#include <cmath>
#include <iostream>
#include <string>

using namespace std;

circle::circle(int radius)
{
  this->radius = radius;
}

string circle::get_Area()
{
  double total{};
  total = pow((radius *), double y)
}
